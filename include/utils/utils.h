#ifndef UTILS_H
#define UTILS_H

#include <bits/stdc++.h>

#define ll int
#define str string
#define db double
#define sz(s) ((ll) s.size())
#define pii pair<ll, ll>
#define fi first
#define se second

db r2();
std::str to_str(ll k);

#endif // UTILS_H